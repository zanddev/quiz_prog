Questo programma permette di eseguire gli spezzoni di codice presenti nelle
slide e copiati all'interno dei file di libreria .h.

Si evidenzia il fatto che il programma effettua una mera esecuzione di tali
spezzoni, quindi in caso l'output sia indefinito il programma restituisce
comunque un valore (non accettabile).


vedere select.h per il programma
  see  select.h for the program

Risposte_prog.txt contiene i risultati degli esami
Quiz_prog.txt contiene i risultati dei quiz

Risposte_prog.txt contains the exam answers
Quiz_prog.txt contains the quiz answers
